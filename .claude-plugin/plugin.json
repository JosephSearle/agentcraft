{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "agentcraft",
  "displayName": "AgentCraft",
  "version": "1.0.0",
  "description": "Production-grade Claude plugin for building, evaluating, and deploying agents with the LangChain ecosystem — LangGraph, LangSmith, RAG pipelines, multi-agent topologies, MCP tools, and Python engineering standards.",
  "author": {
    "name": "Joseph Searle",
    "email": "josephwilliamsearle@gmail.com"
  },
  "repository": "https://github.com/josephsearle/agentcraft",
  "license": "MIT",
  "keywords": [
    "langchain",
    "langgraph",
    "langsmith",
    "agents",
    "rag",
    "mcp",
    "multi-agent",
    "llm",
    "python",
    "production"
  ],
  "userConfig": {
    "langsmith_api_key": {
      "type": "string",
      "title": "LangSmith API Key",
      "description": "Your LangSmith API key for tracing, evaluation, and prompt management. Get one at https://smith.langchain.com",
      "sensitive": true,
      "required": false
    },
    "langsmith_project": {
      "type": "string",
      "title": "LangSmith Project Name",
      "description": "The LangSmith project to log traces to (defaults to 'default')",
      "required": false,
      "default": "default"
    },
    "openai_api_key": {
      "type": "string",
      "title": "OpenAI API Key",
      "description": "Optional — required only if using OpenAI or Azure OpenAI models",
      "sensitive": true,
      "required": false
    },
    "anthropic_api_key": {
      "type": "string",
      "title": "Anthropic API Key",
      "description": "Optional — required only if using Anthropic models directly (not via Bedrock)",
      "sensitive": true,
      "required": false
    }
  }
}
